"""The component set and its build-unit graph.

The in-memory form of `infra/data/cangjie.toml` (CFG-2/3, requirements §4.3). Nodes are
build units, not components: one repository may hold several.
"""
